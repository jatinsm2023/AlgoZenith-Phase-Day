#include <bits/stdc++.h>
using namespace std;

#define int long long
const int m = 1e9 + 9;
const int N = 1e6 + 1;
int A[N];
int B[N];
int C[N];
signed main()
{
    int t;
    cin >> t;

    while (t--)
    {
        int a, b, n;
        cin >> a >> b >> n;
        A[0] = 1;
        B[0] = 1;
        C[0] = 1;
        for (int i = 1; i < N; i++)
        {
            A[i]
        }
    }
}